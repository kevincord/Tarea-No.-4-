function calcularTotalProducto1(precio) {
    const impuesto = precio * 0.12;
    return precio + impuesto;
}

function calcularTotalProducto2(precio) {
    const impuesto = precio * 0.12;
    return precio + impuesto;
}
function calcularTotal(precio) {
    const impuesto = precio * 0.12;
    return precio + impuesto;
}